# IDA_CFGBuilder
The idea is to build a CFG similar to the one in x64dbg for dynamic analyse
This script doesn't build normal the CFG as i want, i also need to add a normal layer rendering model. Currently it looks horrible
